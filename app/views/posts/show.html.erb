<p id="notice"><%= notice %></p>

<p>
  <strong>상품:</strong>
  <%= @post.title %>
</p>

<p>
  <strong>가격:</strong>
  <%= @post.content %>
</p>

 
<p>
  <strong>찜:</strong>
  <%= @post.likes.size %> 명이 찜 했습니다. 
</p>
 
<p>
  <% if current_user.is_like? (@post) %>
  <form action='/post/<%= @post.id%>/like' method='post'>
  <button> 찜 취소 </button>  
  </form>
  
  <% else %>
  <form action='/post/<%= @post.id%>/like' method='post'>
  <button> 찜 </button>  
  </form>
  
  <% end %>
</p>


<%= link_to 'Edit', edit_post_path(@post) %> |
<%= link_to 'Back', posts_path %>
