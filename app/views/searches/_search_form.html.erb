<%= form_tag(searches_result_path, method: "get") do %> 
    
<%= text_field_tag(:search_text) %> 
    <%= hidden_field_tag(:bulletin, "#{post}") %> 
  <button type="submit_tag" class="btn btn-outline-warning btn-sm"><i class="fa fa-search" aria-hidden="true"></i>
</button>
<% end %>
